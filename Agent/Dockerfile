# DEV/DEMO ONLY: Python agent stub
FROM python:3.10-slim
WORKDIR /app
COPY . .
# For demo/dev only: no production agent logic
CMD ["python", "workflow.py"]
# Uncomment and configure for production
# CMD ["python", "workflow_prod.py"]
